// import express , { Router } from "express";
// import { fetchPolicy, handleAddPolicy, handleDeletePolicy, handleUpdatePolicy } from "../controllers/policiesControllers.js";
// import { addPolicyVal, delPolicyVal, upPolicyVal } from "../../../utils/validation.js";
// const app = express();
// const router = Router();

// app.get("/fetch-policy" , fetchPolicy);
// app.post("/add-policy" ,addPolicyVal , handleAddPolicy);
// app.delete("/delete-policy" , delPolicyVal , handleDeletePolicy);
// app.put("/update-policy/:id" ,upPolicyVal , handleUpdatePolicy );
// app.use("/", router);
// export default app;
